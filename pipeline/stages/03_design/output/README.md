# Stage 03 — Design · output

The handoff channel to **stage 04**. Stage 03 writes here:

- `design.md` — section-by-section layout + component choices + brand mapping + motion plan
  + responsive behaviour + accessibility commitments

_Tracked via this README so the handoff folder survives a template copy — git does not track empty directories._
