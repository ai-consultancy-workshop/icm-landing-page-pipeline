# Stage 05 — Review & Merge · output

The final stage's record. Stage 05 writes here:

- `review-report.md` — QA results (lint/types/build/a11y/perf), brand- and spec-fidelity
  findings, what was fixed vs deferred, and the PR link

Stage 05 also opens a **PR in this repo** with the landing page ready to merge.

_Tracked via this README so the folder survives a template copy — git does not track empty directories._
