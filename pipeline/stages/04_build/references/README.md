# Stage 04 — references (Layer 3)

This stage has **no stage-specific references**. Its stable rules are
`pipeline/_config/engineering-conventions.md` plus the `next-best-practices` skill
(re-read at build time, per the stage contract).

Reserved Layer-3 slot, kept so the stage structure stays uniform and survives a
template copy (git does not track empty directories).
