# Stage 04 — Build · output

The handoff channel to **stage 05**. Stage 04 writes here:

- `build-notes.md` — what was built, decisions/trade-offs, anything deferred, and which
  sections need a human eye

> The Next.js app itself is built at the **repo root** (`app/`, `components/`, theme/token
> files, assets) — **not** in this folder. Only the build notes live here.

_Tracked via this README so the handoff folder survives a template copy — git does not track empty directories._
