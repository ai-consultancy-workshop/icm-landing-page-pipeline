# Stage 00 — references (Layer 3)

This stage has **no stage-specific references**. Its stable rules live in
`pipeline/setup/questionnaires/` (`_router.md` + the business-type question sets).

Reserved Layer-3 slot, kept so the stage structure stays uniform and survives a
template copy (git does not track empty directories).
