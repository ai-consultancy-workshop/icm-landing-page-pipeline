# Stage 00 — Intake · output

The handoff channel to **stage 01**. Stage 00 writes here; stage 01 reads it by name.

- `business-profile.json` — structured intake facts (`entry_path`, brand, goal, CTA, contact, …)
- `intake.md` — the same in prose: who this business is and what the page must achieve

_Tracked via this README so the handoff folder survives a template copy — git does not track empty directories._
