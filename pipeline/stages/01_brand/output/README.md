# Stage 01 — Brand Identity · output

The handoff channel to **stages 02–05**. Stage 01 writes here:

- `tokens.json` — palette (OKLCH), typography, radius, spacing, per `_config/tokens-schema.md`
- `brand-identity.md` — voice, personality, do/don't, visual character (and, on Path A, kept-vs-changed)
- `assets/` — logo + reference screenshot(s) when available

_Tracked via this README so the handoff folder survives a template copy — git does not track empty directories._
