# Stage 02 — Spec · output

The handoff channel to **stages 03–05**. Stage 02 writes here:

- `spec.md` — objective, target conversion, ordered information architecture with a
  per-section content outline, SEO/metadata, and CTA strategy

_Tracked via this README so the handoff folder survives a template copy — git does not track empty directories._
