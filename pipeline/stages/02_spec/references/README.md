# Stage 02 — references (Layer 3)

This stage has **no stage-specific references**. Its stable rules are the shared
references in `pipeline/_config/` — `design-principles.md` and `section-catalog.md`.

Reserved Layer-3 slot, kept so the stage structure stays uniform and survives a
template copy (git does not track empty directories).
